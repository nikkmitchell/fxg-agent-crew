node_modules
dist
dist-server
output
.playwright-cli
.DS_Store
*.local
*.log
*.tsbuildinfo
package-lock.json

.claude/settings.json

.claude/launch.json
.harness-run/

# Blobs written by local development and the room harness. Never a deliverable.
.dev-blobs/

# Page stills written by the local renderer. Pictures, not source.
.dev-stills/

# Python bytecode. Committed once by accident when a `git add -A` swept up the
# cache an import test had just created.
__pycache__/
*.pyc
